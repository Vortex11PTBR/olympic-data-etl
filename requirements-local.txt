# Local Development Requirements (Windows-friendly)
# Reduced set for testing without full GCP dependencies

# Data Processing (Windows-compatible versions)
pandas>=2.0.0
pyarrow>=10.0.0

# APIs & HTTP
requests==2.31.0
httpx==0.25.2

# Data Quality
great-expectations==0.18.4

# Google Cloud (pre-compiled)
google-cloud-bigquery==3.17.1
google-cloud-storage==2.10.0
google-auth==2.27.0

# Config & YAML
pyyaml==6.0.1

# Utilities
python-dotenv==1.0.0
