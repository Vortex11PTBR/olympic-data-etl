# Minimal dependencies for GitHub Actions ETL Pipeline
# Only essentials needed to run the pipeline

pandas>=2.0.0
pyarrow>=10.0.0
requests==2.31.0
pyyaml==6.0.1
python-dotenv==1.0.0
google-cloud-bigquery==3.17.1
google-auth==2.27.0
